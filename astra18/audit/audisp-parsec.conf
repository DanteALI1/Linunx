# /etc/audisp/plugins.d/audisp-parsec.conf  (типовой шаблон Astra SE)
# Актуальные пути/имена пакетов сверяйте с документацией Astra Linux SE 1.8.
#
# Плагин передаёт события audit в подсистему PARSEC для корреляции МРД/МКЦ.

active = yes
direction = out
path = /sbin/audisp-parsec
type = always
format = string
args =
